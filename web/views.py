from .models import User, db_session


def test():
    # test code
    # user = User(name="name_1", access_token="at_1", refresh_token="rt_1")
    # db_session.add(user)
    # db_session.commit()
    return {"test": True}
